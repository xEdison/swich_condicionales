let texto = prompt("Ingrese un texto con las palabras ADSO y Desarrolladores");

if (texto.includes("ADSO") && texto.includes("Desarrolladores")) {
    console.log("El texto es valido");
    
} else {
    console.log("El texto no es valido");
}